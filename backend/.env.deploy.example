# Google Cloud Project Configuration
PROJECT_ID="your-gcp-project-id"
REGION="europe-west1"

# Cloud Storage Bucket for public chapter assets (images, audio)
# Must be globally unique across all of Google Cloud
PUBLIC_ASSETS_BUCKET="daskalo-public-assets-bucket-unique-id"

# Firestore Configuration (default is usually '(default)')
DB_NAME="(default)"
